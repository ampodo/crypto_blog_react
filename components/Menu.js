import React from 'react';

const Menu = (x,y, showMenu) => {
    const style = () => {
           return {
                  
                
        };
    };
     return (
        <div style={style()}>
            
        </div>    
  );
};

  const style = {
       
      div: {
             
          },
          margin: {
              
          }
       }

export default Menu
